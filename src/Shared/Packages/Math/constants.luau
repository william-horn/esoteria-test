--!strict

local pi = math.pi
local tau = pi * 2
local zeroVec3 = Vector3.new(0, 0, 0)
local zeroCFrame = CFrame.new(0, 0, 0)

return {
	tau = tau,
	pi = pi,
	zeroVec3 = zeroVec3,
	zeroCFrame = zeroCFrame,
}
