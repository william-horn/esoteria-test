--!strict

local pi: number = math.pi
local tau: number = pi * 2
local zeroVec3: Vector3 = Vector3.new(0, 0, 0)
local zeroCFrame: CFrame = CFrame.new(0, 0, 0)

return {
	tau = tau,
	pi = pi,
	zeroVec3 = zeroVec3,
	zeroCFrame = zeroCFrame,
}
